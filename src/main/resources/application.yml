logging.level.org.hibernate: DEBUG
logging:
  level:
    org:
      hibernate:
        type:
          descriptor:
            sql: trace
spring:
  jpa:
    properties:
      hibernate:
        show_sql: true
        format_sql: true